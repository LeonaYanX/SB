@{
    ViewData["Title"] = "Home Page";
}
@model IEnumerable<Catalog>

<div class="text-center" style="background-size">
   <table class="table">
        @foreach (var c in Model)
        {
            @Html.ActionLink(c.Value.ToString(),"Index","Category");<br/>
            
        }
        
   </table>
</div>
